---
description: Understand a PR fast — paste a PR URL and ask questions about it (why is this here, is it in line with v2, does it break conventions).
argument-hint: <pr-url>
---

Use the `reviewing-a-pr` skill to load and review the pull request at: $ARGUMENTS

If no URL was given, ask for one. This is a conversational companion, not a full review — once the PR is loaded, give a two-line orientation and wait for the user's questions.

It's meant to be snappy: run it on **Sonnet 5**. If we're on a heavier model right now, say so once and suggest `/model` before we dig in.
